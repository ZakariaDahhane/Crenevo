<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> | Crénévo</title>
</head>
<body>
    <header>
        <nav>
            <a href="/">Crénévo</a>
        </nav>
        <% if(userId) { %>
            <a href="/reservations">Mes réservations</a>
            <% } %>
        <% if(userRole === 'manager') { %>
            <a href="/manager/reservations">Gérer les réservations </a>
            <% } %>
    </header>
    <main>